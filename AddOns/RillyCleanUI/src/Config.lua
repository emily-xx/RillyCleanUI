-- Setting any value here to nil will result in Blizzard defaults, if you want those instead.

RCConfig = {
  actionBarOffset = 20,
  castbarOffset = 210,
  nameplateScale = {
    target: 1.3,
    normal: 1.0
  }
}
